# KWS（唤醒词，规划）

## 方向

- 小模型（<1MB）持续监听，CPU 即可（不需要 KPU 常驻抢占 CMA）
- 候选：开源中文唤醒词模型量化到 int8 + RVV 推理；或小核 RT-Thread 侧跑（彻底解放大核）
- 与 ASR 接力：唤醒 → 切 ASR 流式

## 待办

1. 唤醒词选型（"小智"类开源方案对比）
2. 大核 vs 小核部署对比测试（功耗/延迟/内存）
3. 音频前端（mic → VAD → KWS）链路验证
